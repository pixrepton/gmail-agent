---
description: "Use for Cursor/Codex agent harness work: MCP, hooks, verify-pack, agent_harness_audit, context discipline, handover, or live-proof workflow setup. Not for ordinary local edits."
alwaysApply: false
globs:
  - ".cursor/**"
  - "docs/dev/**"
  - "tools/scripts/agent_harness_audit.py"
---

# Agent Harness and MCP

Use this rule only for agent harness, MCP, slash commands, verification loops, context discipline, or handover workflows.

## Prefer Skills First (workspace)

- `../../.agents/skills/cursor-codex-harness/SKILL.md`
- `../../.agents/skills/gmail-agent-proof-run/SKILL.md`
- `../../.agents/skills/policy-action-gates/SKILL.md`
- `../../.agents/skills/agent-skill-quality-gate/SKILL.md` — when creating/editing skills

## MCP and Tooling

- Prefer workspace MCP policy via `../../knowledge/system-atlas/tooling/CODE_INTELLIGENCE_ROUTER.md`.
- Repo-local `gmail-agent-repo-assistant` MCP when available; verify with file reads.
- GitNexus/CBM/Serena: structural hints only, not runtime proof.
- Context7: external library docs only.

## Navigation and Governance

- **Git:** see `00-topinstal-core-router.mdc` (**Git i repo**); workspace task engine: `../../scripts/ai_os_task.py`.
- Harness index: `../../knowledge/system-atlas/tooling/agent-harness/AGENT_DEVELOPMENT_HARNESS.md`.
- Skills registry: `../../knowledge/system-atlas/tooling/agent-harness/AGENT_SKILLS_REGISTRY.md`.
- Handoff: `../../knowledge/SESSION_MEMORY_POLICY.md` and `../../knowledge/memory/LAST_SESSION.md` (operator instruction to write).

## Proof and Boundaries

- On ambiguous infra failures, contrast hypotheses and falsify with evidence.
- Before substantive code done: minimal checks from `AGENTS.md` and selected skills.
- Never claim fresh live Gmail/Daszek success or Gate B passed from harness docs alone.
- Respect Node A vs Node B: bridge/API, not shared filesystem.

## `.cursor/rules` Markdown link hygiene

- Links in `.mdc` resolve relative to `.cursor/rules/`. Use `../../` for workspace and repo-root targets.
- After editing links, from workspace root run:

```powershell
python scripts/context_link_audit.py --scope gmail-agent
python scripts/agent_harness_audit.py
```

## Future-proofing

- Recurring harness failures → update workspace `agent-harness/` docs or this rule, not chat-only guidance.

## Verification

```powershell
# from workspace root
python scripts/context_link_audit.py --scope gmail-agent
python scripts/agent_harness_audit.py
python gmail-agent/tools/scripts/agent_harness_audit.py
```
